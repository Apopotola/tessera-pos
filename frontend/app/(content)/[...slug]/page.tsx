/**
 * Catch-all for workspace URLs (/dashboard, /inventory/stock, …).
 * The (content) layout renders the matching tab; WorkspaceUrlSync maps the path to a menu viewType.
 */
export default function WorkspaceRoute() {
  return null;
}
